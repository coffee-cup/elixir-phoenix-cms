<%= render WritingWeb.SharedView, "header.html", Map.put(assigns, :header_title, "New Article") %>

<%= render "form.html", Map.put(assigns, :action, article_path(@conn, :create)) %>

<span><%= link "Back", to: article_path(@conn, :index) %></span>
