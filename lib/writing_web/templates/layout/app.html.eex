<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">

    <%= if Map.has_key?(assigns, :title) do %>
      <title><%= Map.get(assigns, :title) %></title>
    <% else %>
      <title>w.</title>
    <% end %>

    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="icon" type="image/png" href="/favicon-32x32.png?v=2" sizes="32x32">
    <link rel="icon" type="image/png" href="/favicon-16x16.png?v=2" sizes="16x16">
    <link rel="shortcut icon" href="/favicon.ico?v=2">
    <link rel="author" href="humans.txt" />

    <!-- Search Engine -->
    <meta name="description" content="A collection of articles about things I find interesting.">
    <!-- Schema.org for Google -->
    <%= if Map.has_key?(assigns, :title) do %>
      <meta itemprop="name" content="<%= Map.get(assigns, :title) %>">
    <% else %>
      <meta itemprop="name" content="w.">
    <% end %>
    <meta itemprop="description" content="A collection of articles about things I find interesting.">

    <!-- Twitter -->
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="w.">
    <meta name="twitter:description" content="A collection of articles about things I find interesting.">
    <meta name="twitter:creator" content="jakerunzer">

    <!-- Open Graph general (Facebook, Pinterest & Google+) -->
    <%= if Map.has_key?(assigns, :title) do %>
      <meta name="og:title" content="<%= Map.get(assigns, :title) %>">
    <% else %>
      <meta name="og:title" content="w.">
    <% end %>
    <meta name="og:description" content="A collection of articles about things I find interesting.">
    <meta name="og:url" content="https://elm-phoenix-template.herokuapp.com/">
    <meta name="og:site_name" content="w.">
    <meta name="og:type" content="article">
    <%= if Map.has_key?(assigns, :image) do %>
      <meta name="og:image" content="<%= Map.get(assigns, :image) %>">
    <% end %>

    <meta name="HandheldFriendly" content="True">
    <meta name="MobileOptimized" content="320">

    <link rel="stylesheet" href="<%= static_path(@conn, "/app.css") %>">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Merriweather:400,400i,700">

  </head>

  <body>
    <div class="container">
      <main role="main" class="full">
        <%= render @view_module, @view_template, assigns %>
      </main>

    <%= if @is_admin do %>
      <%= render WritingWeb.SharedView, "admin_footer.html", assigns %>
    <% else %>
      <%= render WritingWeb.SharedView, "footer.html", assigns %>
    <% end %>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/app.js") %>"></script>

    <%# hightlight.js %>
    <script src="<%= static_path(@conn, "/highlight.pack.js") %>"></script>
    <link rel="stylesheet" href="<%= static_path(@conn, "/github-gist.css") %>">
    <script>hljs.initHighlightingOnLoad();</script>
  </body>
</html>
